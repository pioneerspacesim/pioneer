#pragma once
#ifndef USING_H_
#define USING_H_

#include <string>
#include <vector>
#include <iostream>

#include "fixed.h"

using str = std::string;
using std::cout;
using std::cin;
using std::endl;
using std::vector;
using std::array;
using std::swap;

using shint = short int;
using sh = short;
using lg = long;
using ll = long long;
using dbl = double;
using flt = float;
using ldbl = long double;
using bl = bool;
using vd = void;
using chr = char;
using ui3t = uint32_t;
using ui6t = uint64_t;

using cshint = const short int;
using csh = const short;
using cint = const int;
using clg = const long;
using cll = const long long;
using cdbl = const double;
using cflt = const float;
using cldbl = const long double;
using cbl = const bool;
using cvd = const void;
using cchr = const char;
using cstr = const str;
using cui3t = const uint32_t;
using cui6t = const uint64_t;

using ui3 = Uint32;
using ui6 = Uint64;
using cui3 = const Uint32;
using cui6 = const Uint64;
using si3 = Sint32;
using si6 = Sint64;
using csi3 = const Sint32;
using csi6 = const Sint64;

using fx = fixed;

using cstr = const str;

using ui = unsigned int;
using cui = const unsigned int;

#endif
